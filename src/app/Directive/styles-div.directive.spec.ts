import { StylesDivDirective } from './styles-div.directive';

describe('StylesDivDirective', () => {
  it('should create an instance', () => {
    const directive = new StylesDivDirective();
    expect(directive).toBeTruthy();
  });
});
